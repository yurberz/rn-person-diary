import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  imageBackground: {
    width: 90,
    height: 90,
  },
  image: {
    borderRadius: 15,
  },
  icon: {
    position: 'absolute',
    opacity: 1,
    overflow: 'hidden',
    right: 4,
    bottom: 3,
  },
});

export default styles;